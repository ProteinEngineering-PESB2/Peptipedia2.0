version: "3.3"
services:
  backend:
    build: ./backend/
    volumes:
      -  ./backend/src:/app/src
    ports:
      - "8001:8001"
  app :
    build: ./app/
    volumes:
      - ./app/public:/app/public
      - ./app/src:/app/src
    depends_on:
      - "backend"
    ports:
    - "3000:3000"